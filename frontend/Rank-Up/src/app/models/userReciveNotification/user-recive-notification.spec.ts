import { UserReciveNotification } from './user-recive-notification';

describe('UserReciveNotification', () => {
  it('should create an instance', () => {
    expect(new UserReciveNotification()).toBeTruthy();
  });
});
